# Test dependencies for AgentCore Gateway Lambda functions
pytest>=7.0.0
pytest-asyncio>=0.21.0

# Lambda function dependencies (for testing)
arxiv>=2.0.0
boto3>=1.34.0
googlemaps>=4.2.0,<5.0.0

# Linting
ruff>=0.9.0
